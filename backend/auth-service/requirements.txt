# --- Framework webowy i serwer ASGI ---
fastapi==0.115.3              # Główny framework do budowy API (Auth Service)
uvicorn[standard]==0.30.6     # Lekki serwer ASGI do uruchamiania FastAPI

# --- Warstwa bazy danych (ORM + migracje + sterownik PostgreSQL) ---
sqlalchemy==2.0.34            # ORM do mapowania modeli (User, itd.) na tabele w DB
psycopg2-binary==2.9.9        # Sterownik PostgreSQL (połączenie z bazą danych)
alembic==1.13.2               # Migracje schematu bazy danych

# --- Walidacja i konfiguracja ---
pydantic[email]==2.9.2        # Walidacja danych + email-validator (wymagane do schematów Pydantic)
pydantic-settings==2.5.2      # Obsługa konfiguracji przez zmienne środowiskowe (Settings)

# --- Bezpieczeństwo i JWT ---
passlib[bcrypt]==1.7.4        # Haszowanie haseł użytkowników (bcrypt)
python-jose[cryptography]==3.3.0  # Generowanie i weryfikacja tokenów JWT

# --- Komunikacja HTTP (np. z innymi mikroserwisami) ---
httpx==0.26.0                 # Asynchroniczny klient HTTP (wywołania między serwisami)
requests==2.31.0              # Popularna biblioteka HTTP (synchron.)

# --- Obsługa multipart/form-data (np. formularze, upload plików) ---
python-multipart==0.0.9       # Parser multipart/form-data dla FastAPI

# --- Testy jednostkowe i pokrycie kodu ---
pytest==7.4.3                 # Testy jednostkowe
pytest-asyncio==0.21.1        # Testy async/await
pytest-cov==4.1.0             # Pokrycie kodu testami
