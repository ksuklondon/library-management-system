# --- Framework webowy i serwer ASGI ---
fastapi==0.115.3               # Główny framework API
uvicorn[standard]==0.30.6      # Serwer ASGI

# --- Baza danych (ORM + migracje + sterownik PostgreSQL) ---
sqlalchemy==2.0.34
psycopg2-binary==2.9.9
alembic==1.13.2

# --- Walidacja i konfiguracja ---
pydantic[email]==2.9.2         # Z email-validatorem
pydantic-settings==2.5.2

# --- Autoryzacja i bezpieczeństwo (WYMAGANE dla shared/dependencies.py) ---
python-jose[cryptography]==3.3.0  # JWT token handling
passlib[bcrypt]==1.7.4            # Password hashing
python-dotenv==1.2.1              # Environment variables

# --- Komunikacja z auth-service i innymi ---
httpx==0.26.0
requests==2.31.0

# --- Obsługa plików i formularzy ---
python-multipart==0.0.9

# --- Harmonogramowanie zadań (np. kary, przypomnienia) ---
apscheduler==3.10.4

# --- Testowanie ---
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
